---
const {name, bio, skills} = Astro.props;
---

<header class="text-center">
  <h1 class="text-3xl font-bold">{name}</h1>
  <p class="mt-3 text-gray-700">{bio}</p>
  <p class="mt-3 text-gray-700">{skills}</p>
</header>
